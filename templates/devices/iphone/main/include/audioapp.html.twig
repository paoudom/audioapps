
<div class="card mb-2">
    <div class="card-header bg-light">
        <h3>{{ audioapp.TrackName }}</h3>
    </div>
  <div class="card-body">
        <div class="row">
            <div class="col-3">
                <img class="img-fluid img-thumbnail" src="{{ audioapp.preferedIcon }}" alt="{{ audioapp.name }}" />
            
            </div>
            <div class="col-9">

            <div class="row">
                <div class="col-12">
                    <ul class="list-unstyled">
                        <li>Developer : {{ audioapp.artistName|title }}</li> 
                        <li>Categorie : {{ audioapp.categorie.groupe|title  }} - {{ audioapp.categorie.nom|title  }}</li>
                        <li>Minimum OS version : {{ audioapp.MinimumOsVersion }}</li>
                        <li>Laste Update : {{ audioapp.CurrentVersionReleaseDate|date('m-d--Y') }}</li>
                        <li>App Size : {{ audioapp.FileSizeBytes }}</li>
                    </ul>
                </div>
            </div>
                


            </div>
        </div>
  </div>
</div>



        

<div class="card mb-2">
    <div class="card-header bg-light">
        <h5>Screenshot</h5>
    </div>
    <div class="card-body">
        <div class="row">
            {% if audioapp.ipadScreenshotUrls is not empty %}
                {% for key, screen in audioapp.ipadScreenshotUrls %}
                    <div class="col-3">
                        <button type="button" class="btn btn-light" data-toggle="modal" data-target="#img_{{ key }}">
                        <img class="img-fluid img-thumbnail" src="{{ screen }}" alt="{{ audioapp.name }}" />
                        </button>
                    </div>
                {% endfor %}
            {% elseif audioapp.screenshotUrls is not empty %}
                {% for key, screen in audioapp.screenshotUrls|keys %}
                    <div class="col-3">
                    <button type="button" class="btn btn-light" data-toggle="modal" data-target="#img_{{ key }}">
                        <img class="img-fluid img-thumbnail" src="{{ screen }}" alt="{{ audioapp.name }}" />
                        </button>
                    </div>
                {% endfor %}
            {% endif %} 
        </div>  
    </div>
</div>

<div class="card  mb-2">
    <div class="card-header bg-light">
        <h5>Patch note</h5>
    </div>
    <div class="card-body">
        {{ audioapp.releaseNotes|markdown }}
    </div>
</div>

<div class="card">
    <div class="card-header bg-light">
        <h5>Description</h5>
    </div>
    <div class="card-body">
        {{ audioapp.description|markdown }}
    </div>
</div>



            {% if audioapp.ipadScreenshotUrls is not empty %}
                {% for key, screen in audioapp.ipadScreenshotUrls %}
                    <div class="modal fade" id="img_{{ key }}" tabindex="-1" role="dialog" aria-labelledby="img_{{ key }}Label" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="card text-center">
                                <div class="card-body">
                                    <img class=" img-thumbnail" src="{{ screen }}" alt="{{ audioapp.name }}" />
                                </div>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                    </div>
                {% endfor %}
            {% elseif audioapp.screenshotUrls is not empty %}
                {% for key, screen in audioapp.screenshotUrls %}
                    <div class="modal fade" id="img_{{ key }}" tabindex="-1" role="dialog" aria-labelledby="img_{{ key }}Label" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-body">    

                            <div class="card text-center">
                                <div class="card-body">
                                    <img class=" img-thumbnail" src="{{ screen }}" alt="{{ audioapp.name }}" />
                                </div>
                            </div>
                            

                                

                                
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                    </div>
                {% endfor %}
            {% endif %} 
        
        







            




            

    
    





